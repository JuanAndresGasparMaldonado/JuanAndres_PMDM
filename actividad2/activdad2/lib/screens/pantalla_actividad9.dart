import 'package:flutter/material.dart';

class PantallaActividad9 {
    @override  
    Container build(BuildContext context) {
      return Container(
        color: Colors.green,
        child: const Center(
          child: Text(
            'Pantalla de la Actividad 9',
            style: TextStyle(fontSize: 24, color: Colors.white),
          ),
        ),
      );
    }
}